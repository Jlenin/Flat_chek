# Архитектура flat_check_modular

Этот документ — карта того, что откуда грузится и почему. Если нужно найти
код конкретной проверки/флага — смотрите таблицу «Где что искать» в конце.

## Зачем это отдельная версия

`flat_check.sh` (health, 3.8.10) и `flat_check_2.sh` (health + сборщик логов,
3.11.10) — два почти-дублирующих файла на 3300 и 9000+ строк, которые нужно
было держать в ручном «паритете»: любое исправление приходилось вносить в
2-3 места одинаковым патчем. Это была причина почти всех багов, найденных
перед началом этой работы (см. `CONTEXT.md` в корне репозитория).

`flat_check_modular/` — тот же функционал (health check + JSON/push-агент +
сборщик логов + интерактивный мастер), но с одним источником истины на
функцию, разложенным на слои по `lib/`. Оригиналы (`flat_check.sh`,
`flat_check_2.sh`, `agent/`) не менялись и продолжают работать как раньше —
это две параллельные, независимые поставки одного и того же инструмента.

Интерфейс командной строки (все флаги: `-h`, `-i`, `-r`, `-v`, `--json`,
`--push`, `--config`, `--dev`, `--selftest`, `-log -on/-off` и т.д.) не
менялся ни на один флаг — см. `lib/core/09_argv.sh`. Что изменилось — это
только то, из скольких файлов собран код и как он подключается.

## Три слоя + точка входа

```
┌─────────────────────────────────────────────────────────────┐
│  flat_check  (точка входа)                                  │
│  парсит argv → решает, что из lib/ подключить → диспетчер   │
└───────────────┬───────────────────┬──────────────────┬──────┘
                │                    │                  │
                ▼                    ▼                  ▼
        lib/core/ (всегда)    lib/agent/ (если     lib/logging/ (если
        health-check,         --json/--push/       -log/-i/--dev/
        каталог пакетов,      --config/--dev/      --selftest)
        вывод, i18n,          --selftest)
        argv, мастер,
        selftest
```

| Слой | Когда грузится | От чего зависит | Кто от него зависит |
|------|-----------------|------------------|----------------------|
| `lib/core/` | всегда | ничего (кроме bash/coreutils) | agent и logging — оба |
| `lib/agent/` | `--json` / `--push` / `--config` / `--dev` / любой `--selftest` / `-i` | только `lib/core` | никто |
| `lib/logging/` | `-log` / `-i` / `--dev` / любой `--selftest` | только `lib/core` | никто |

**Циклов нет.** `lib/agent` и `lib/logging` не знают друг о друге и не
вызывают функции друг друга — это проверено `grep`'ом по фактическим вызовам
функций между секциями в оригиналах, а не только по комментариям (см.
историю: план декомпозиции содержал точный список пересечений). `lib/core`
не знает ни о `lib/agent`, ни о `lib/logging`.

### Почему `--selftest`/`--dev`/`-i` грузят ОБА опциональных слоя

Самотест — это проверка целостности всей ПОСТАВКИ, а не только того режима,
с которым его запустили: `lib/core/11_selftest.sh` на уровне `simple` уже
проверяет, что `build_health_json`/`push_health_json` определены (agent), а
на уровне `extended` дополнительно гоняет `parse_duration`/seek+chunk
extract (logging). Если бы `--selftest simple` не подключал `lib/agent`, он
не смог бы даже сообщить, что agent вообще на месте.

Мастер (`-i`) устроен аналогично: сам мастер решает уже ПОСЛЕ старта, в
какой режим идти (health / сбор логов / самотест) — если бы `lib/agent`/
`lib/logging` подключались по флагам ДО входа в мастер, часть его пунктов
меню осталась бы без нужных функций.

## Точка входа: `flat_check`

Псевдокод (реальный файл см. `flat_check` в этой же папке):

```bash
SCRIPT_DIR=... ; LIB_DIR="$SCRIPT_DIR/lib"
shopt -s nullglob

for f in "$LIB_DIR"/core/*.sh; do source "$f"; done   # core — всегда

parse_args "$@"     # только разбирает флаги, ничего не запускает

# по разобранным флагам решаем, что ещё подключить
[[ ...-log/-i/--dev/--selftest... ]] && for f in "$LIB_DIR"/logging/*.sh; do source "$f"; done
[[ ...--json/--push/--config/--dev/--selftest/-i... ]] && for f in "$LIB_DIR"/agent/*.sh; do source "$f"; done

dispatch_main "$@"  # тонкий диспетчер: -i / --selftest / --dev / --json|--push / -log / health по умолчанию
```

Никакой динамической магии: ни `eval`, ни автозагрузчиков по имени функции,
ни реестров плагинов — обычный `source` по списку файлов и `if` по уже
распарсенным флагам. `nullglob` нужен, чтобы `lib/agent/*.sh`/`lib/logging/*.sh`
не подставлялись буквально, если бы (гипотетически) каталога не было.

`SCRIPT_DIR`/`SCRIPT_NAME` вычисляются **в самой точке входа**, а не в
`lib/core/00_globals.sh` — если бы это делал lib-файл, `${BASH_SOURCE[0]}`
указывал бы на путь этого lib-файла, а не на `flat_check`.

## Права на исполнение (`chmod +x`)

Только 4 файла верхнего уровня когда-либо запускаются напрямую: `flat_check`,
`install.sh`, `reinstall.sh`, `uninstall.sh`. Всё остальное в `lib/**/*.sh`
только подключается через `source` — **исполняемый бит им не нужен в
принципе**. Самопочинка `+x` (тот же приём, что в `agent/install_flat_check.sh`)
встроена во все четыре верхнеуровневых скрипта: после первого ручного
`chmod +x install.sh` (или запуска как `bash install.sh`) остальное чинится
само при каждом install/reinstall/uninstall.

## Где что искать

| Что | Файл |
|-----|------|
| Флаги, `usage()`, разбор argv, финальный диспетчер | `lib/core/09_argv.sh` |
| Глобальные переменные/дефолты (включая CLI-флаги `-i`/`-log`) | `lib/core/00_globals.sh` |
| Каталог пакетов (`_pkg_set`, builtin fallback, загрузка `conf/flat_check.packages.conf`) | `lib/core/01_catalog.sh` |
| `[OK]/[WARN]/[FAIL]/[INFO]`, сессионный лог, `die()` | `lib/core/02_output.sh` |
| Локализация (`_l`, RU/EN) | `lib/core/03_i18n.sh` |
| Определение ОС/пакетного менеджера | `lib/core/04_os_detect.sh` |
| CPU/память/диск/БД/сеть/сертификаты/uptime (`=== System ===`) | `lib/core/05_system_metrics.sh` |
| Host-wide resource-gate (используют и пакетные проверки, и offline-сборщик) | `lib/core/06_resource_gate.sh` |
| Проверки состояния пакета/службы (порты, API, конфиги, процесс) | `lib/core/07_pkg_checks.sh` |
| `=== Infrastructure ===`, `=== Depends ===`, репозитории, `=== Summary ===` | `lib/core/08_infra_checks.sh` |
| Интерактивный мастер (`-i`) | `lib/core/10_wizard.sh` |
| `--selftest simple\|extended` | `lib/core/11_selftest.sh` |
| Конфиг агента (`--config`, безопасный парсинг инлайн-комментариев) | `lib/agent/01_config.sh` |
| Сборка JSON v2 (`build_health_json` и все `_json_collect_*`) | `lib/agent/02_json_build.sh` |
| Отправка на `PUSH_URLS` (curl, ретраи, диагностика) | `lib/agent/03_push.sh` |
| TUNABLES сборщика логов (лимиты/seek/zgrep/archive) | `lib/logging/00_tunables.sh` |
| Парсеры длительности/момента времени | `lib/logging/01_time_filters.sh` |
| Поиск каталогов логов FLAT-пакетов, `--list-targets` | `lib/logging/02_log_discovery.sh` |
| Поиск логов PostgreSQL | `lib/logging/03_postgres_discovery.sh` |
| Извлечение диапазона из одного файла | `lib/logging/04_extract_single.sh` |
| Извлечение по всем файлам ротации службы | `lib/logging/05_extract_multi.sh` |
| Применение извлечения к найденным директориям (seek/bisect, прогресс, пул воркеров) | `lib/logging/06_extract_apply.sh` |
| Online tail -F, сигналы, безопасная очистка рабочего каталога | `lib/logging/07_collector.sh` |
| `run_log_collection()` (online/offline режимы верхнего уровня) | `lib/logging/08_online_offline.sh` |

Каждый файл в `lib/**/*.sh` начинается с единого заголовка-документации
(Модуль / Слой / Назначение / Публичные функции / Зависит от / Не зависит
от / Side effects / Источник) — открывайте файл, если нужны детали за
пределами этой таблицы.

## Каталог пакетов и зависимости — не трогать бездумно

`conf/flat_check.packages.conf` — копия каталога продуктов/пакетов из корня
репозитория, скопирована как есть. Это данные (`_pkg_set` + `PKG_DEPS`), а
не код — при любых изменениях в этом файле сохраняйте существующие связи
«пакет → продукт → зависимости»; новые записи добавлять можно, существующие
менять — только осознанно и с пониманием, что от них зависит текстовый
`=== Depends ===` и JSON `"infrastructure"`.

## Обнаруженные при переносе расхождения между `flat_check.sh`/`flat_check_2.sh`/`agent/json_report.inc.sh`

Оригиналы не редактировались в рамках этой работы, но при построчной сверке
модульной сборки с ними нашлись места, где копии разошлись (естественный
результат ручного «паритета» несколькими патчами). В модульную сборку взята
исправленная/более полная версия в каждом случае — см. комментарии в
соответствующих файлах:

1. `agent/json_report.inc.sh` не пропускал инфраструктурные пакеты
   (nginx/postgresql/mariadb) через `_is_infrastructure_pkg()` в блоке
   `directories` — показывал бы выдуманные пути `/opt/flat/<pkg>` со
   статусом `"missing"` вместо `"n/a"`. См. `lib/agent/02_json_build.sh`.
2. `flat_check.sh` не звал `cleanup()` из `die()` — в режиме `-log` фатальная
   ошибка оставляла бы недоделанный рабочий каталог. См. `lib/core/02_output.sh`.
3. `relocate_log_to_workdir()` в `flat_check.sh` отсутствует вовсе (health-only
   скрипт не создаёт рабочий каталог архива). См. `lib/logging/08_online_offline.sh`.

Это находки для отдельных тикетов на сами оригиналы — они умышленно не
трогались в рамках декомпозиции (см. README.md в корне репозитория).

## Проверка эквивалентности при дальнейших правках

1. `bash -n` на изменённый файл и на `flat_check`.
2. `./flat_check --selftest extended` — должен пройти без `[FAIL]`.
3. Сравнить вывод с оригиналом на одном и том же состоянии хоста:
   - `./flat_check_modular/flat_check --json` vs `./flat_check.sh --json`
     (сравнивать по ключам, кроме заведомо переменных — `timestamp`,
     `system.cpu`, `system.memory`, `system.uptime_seconds`, `certificates`);
   - обычный текстовый вывод — построчно;
   - `-log -off` на одном и том же наборе логов — список файлов в архиве.
